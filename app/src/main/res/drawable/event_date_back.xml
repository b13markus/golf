<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">

    <solid
        android:color="@color/btnColor" />

    <stroke
        android:width="1dp"
        android:color="@color/btnColor" />

    <padding
        android:left="@dimen/event_padding"
        android:top="@dimen/event_padding"
        android:bottom="@dimen/event_padding"
        android:right="@dimen/event_padding"/>

    <corners
        android:radius="@dimen/event_radius" />

</shape>